/**
 * Infinite canvas plugin, node half. The browser implementation is loaded
 * through the package's `./client` entry by the web profile.
 */

/** Host plugin body — the canvas is browser-only. */
export function apply(): void {}
